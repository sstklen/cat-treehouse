# 🐱 cat Treehouse 抽獎 — 公開可驗證開獎協議

## 1. 中獎名額
- **1 名**（一人中獎、四人同行：中獎者 + 3 位朋友）

## 2. 名單承諾 (Commitment — 開獎前公布)
- 抽獎池：**231 筆報名**（已排除主辦 #17、9 筆無票測試）
- 總票數：**219,367 張**
- 計票基準：照各報名 GitHub issue 實際記錄（早鳥保底 + GitHub stars + Claude Max +200 + 已記錄之推薦），**不事後追加**
- 名單檔：`roster.csv` / `ROSTER.md`（含每筆票號區間）
- **承諾雜湊 SHA256 = `0dfd15aebb26d75e382fb0060023d73c64c9cf93f9662a45a795989695458aab`**
- 承諾時間：2026-06-07（開獎種子尚未產生之前）

## 3. 隨機種子 (Beacon — 開獎後才知道、不可預測)【v2 已鎖死，消除挑選空間】
- 指定彩券：**台灣彩券「威力彩」**（唯一，不提供彩種選項）
- 指定期別：**2026-06-08（含）之後第一個威力彩開獎期**，由台灣彩券官網客觀認定；
  僅在官網明確未公告該期時，順延至下一個威力彩開獎日（主辦無裁量權）
- **正準種子（程式強制，唯一寫法）**：
  `seed = 威力彩|2026-06-08|<第一區6號：由小到大、各補零2位、逗號連接無空白>|<第二區1號：補零2位>`
  範例（號碼僅為示意）：`威力彩|2026-06-08|03,11,18,24,29,38|07`
- **關鍵**：操作者只輸入 7 個官方號碼，格式/日期/彩種全部寫死在 `draw.py` 內並已承諾，
  同一組號碼不論輸入順序或補零方式都會正規化成同一字串 → 無法靠寫法挑中獎者。

## 4. 開獎演算法（任何人可重跑）
```
H        = SHA256( ROSTER_SHA256 + ":" + seed )
winning  = int(H, 16) mod 219367 + 1     # 取得中獎票號 1..219367
winner   = roster 中票號區間 [start,end] 涵蓋 winning 的那一筆
```
- 程式：`draw.py`，用法：`python3 draw.py 03 11 18 24 29 38 07`（輸入威力彩開獎 7 號）
- 決定性：同樣的名單 + 同樣的號碼 → 永遠得到同一位中獎者
- 防竄改：`draw.py` 開獎前會重算名單指紋並比對承諾，不符即拒絕開獎

## 4.1 雙重承諾雜湊（開獎前公布，見 COMMITMENT.txt）
- 名單承諾 `ROSTER_SHA256 = 0dfd15aebb26d75e382fb0060023d73c64c9cf93f9662a45a795989695458aab`
- 演算法承諾 `DRAW_PY_SHA256 = b9d004e567911ef5283eeb130641e41389cb88e26193d75ee29f290472710dbe`

## 5. 為什麼公平、不可操弄
1. **先承諾後開獎**：名單與雜湊在種子產生「之前」就公布；事後改任何一筆，雜湊就對不上。
2. **種子不可預測**：台灣彩券開獎號全國公開、主辦方無法預知或操控。
3. **完全可驗證**：任何參與者可自行下載名單、確認雜湊、輸入開獎號重跑 `draw.py`，得到相同結果。
4. **票數可逐筆查核**：每筆票數對應一個公開 GitHub issue，可獨立核對。

## 6. 已知限制（透明揭露）
- **真實 Email 不可得**：系統設計只保留遮罩 email + 雜湊，原始 email 從未落地（GitHub/主機/CF/TollBit 皆無）。公開名單以遮罩 email + issue 編號識別；中獎者請以本人 email + 對應 issue 票數自證。
- **推薦未追溯回補**：109 筆推薦中 76% 為無法歸戶的 email-hash，完整回補技術上不可能；為求人人平等，採「照實際記錄」計票（與每位報名當下看到的票數一致）。
- **同帳號重複**：@harvey3630-hue 有 2 筆報名（依主辦決定保留）；Email 報名因遮罩無法偵測重複，但報名系統有 localStorage + 雜湊去重。
